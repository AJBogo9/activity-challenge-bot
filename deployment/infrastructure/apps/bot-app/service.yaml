apiVersion: v1
kind: Service
metadata:
  name: activity-challenge-bot
  namespace: bot-system
spec:
  selector:
    app: activity-challenge-bot
  ports:
    - name: http
      port: 3001
      targetPort: 3001
  type: ClusterIP
